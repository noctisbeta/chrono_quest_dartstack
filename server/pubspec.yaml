name: server
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  common:
    path: ../common
  crypto: ^3.0.6
  dart_frog: ^1.1.0
  dotenv: ^4.2.0
  meta: ^1.16.0
  postgres: ^3.5.0
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  flutter_lints: ^5.0.0
  mocktail: ^1.0.3
  test: ^1.25.5
  very_good_analysis: ^5.1.0
